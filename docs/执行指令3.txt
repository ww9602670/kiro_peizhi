基于 shared_worker_timing_design.md，拆分可执行任务清单。

本轮仍然禁止修改代码。

请输出：

docs/remediation/shared_worker_timing_task_plan.md

设计任务时，尽量设计拆分成互不冲突可并列执行的任务，安排agent gpt5.3codex执行并进行阀门自检，pass后再向你汇报

每个任务必须包含：

- 任务编号
- 修改目标
- 涉及文件
- 涉及函数
- 修改说明
- 禁止事项
- 验收方式
- 回滚风险
- 是否需要前端联动
- 是否需要后端测试


当所有的任务都执行完毕后，你负责审核，测试，git，部署（部署时检测时候有策略在运行，如有在运行直接阻塞上报，暂停更新），服务器上执行更新自测，使用shun666的账号。允许真实下注（AI推荐平注，球1，金额1，时机默认）进行测试检测，主要观测本轮所更新所有内容的是否生效符合预期，并最终给出一份完整的报告性回复