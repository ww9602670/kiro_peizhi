先不要改任何代码。

请根据 "H:\d\bocai_web\docs\shared-market-worker-timing-remediation-report.md"，核查当前项目代码中与以下链路相关的真实实现：

1. 共享采集器频率。
2. 开奖倒计时归零后的刷新机制。
3. worker 获取期号、开奖、倒计时的来源。
4. worker 下注前是否调用第三方平台倒计时接口。
5. 共享快照缺失、过期、异常时是否会触发操作者账号本地采集。
6. 本地采集结果是否可能写回共享池。
7. 结算是否阻塞 worker 主循环。
8. 策略创建、编辑、启动、重启恢复是否依赖 verification_stale。
9. 账号被顶后的自动重登逻辑。
10. 前端开奖倒计时归零后的刷新逻辑。

然后请输出：

docs/remediation/phase1_current_chain_report.md

报告必须包含：

- 文件路径
- 函数名
- 当前逻辑
- 是否符合整改报告
- 证据位置
- 风险判断
- 后续修改建议

执行完毕后读取并执行"H:\d\bocai_web\docs\执行指令2.txt"
